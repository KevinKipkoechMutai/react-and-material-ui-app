import React from "react"
import { Button, Typography } from "@mui/material"

export default function App() {
    return (
        <>
            <Button variant="contained">Hello World!</Button>
            <Typography variant="h4">This is a text</Typography>
        </>
    )
}